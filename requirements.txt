# --- ranking step (CPU, offline) ---
numpy>=1.24
scikit-learn>=1.3

# --- offline precompute only (NOT used during the 5-min ranking step) ---
sentence-transformers>=2.7.0

# --- sandbox demo only ---
streamlit>=1.33
